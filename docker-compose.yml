version: '3.8'

services:
  # Service untuk aplikasi Flask kita
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis

  # Service untuk database Redis
  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"